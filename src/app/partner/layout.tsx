import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Partner",
  description: "Work with PSM Infinity. Studio partnerships for branding, technology, textile, and logistics in Kolkata.",
};

export default function PartnerLayout({ children }: { children: React.ReactNode }) {
  return children;
}

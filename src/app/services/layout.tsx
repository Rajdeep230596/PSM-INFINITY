import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services",
  description: "Personalised branding, corporate identity, technology, textile, and logistics from PSM Infinity in Kolkata.",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
